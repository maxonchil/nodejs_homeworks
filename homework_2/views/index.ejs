<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Be3x fanpage</title>
  </head>
  <body>
    <%if(home) {%> 
      <%-include('home'); -%>
      <% } else if (login) {%>
         <%-include('loginPage'); -%>
         <% }else  {%>
          <%-include('userPage'); -%>
          <% }%>
  </body>
</html>
